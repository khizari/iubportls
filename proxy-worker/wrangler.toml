name = "iub-portal-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# Create this with: wrangler kv:namespace create SESSIONS
# then paste the returned id below.
[[kv_namespaces]]
binding = "SESSIONS"
id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID"
